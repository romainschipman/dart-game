import { StyleSheet } from "react-native";

const keyboardItemStyles = StyleSheet.create({
    container: {
        margin: 5,
        justifyContent: "center",
        alignItems: "center",
        backgroundColor: "#ffffff",
    },
    text: {
        fontSize: 16,
        letterSpacing: 2
    }
});

export { keyboardItemStyles };